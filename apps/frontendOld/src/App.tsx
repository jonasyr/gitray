import MainPage from './pages/MainPage';
import './App.css';

/**
 * Root component rendered by `main.tsx`. Acts as the entry point for the
 * single-page application.
 */

/**
 * Simple wrapper component that renders the `MainPage` component.
 */
function App() {
  return <MainPage />;
}

export default App;
